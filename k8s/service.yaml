apiVersion: v1
kind: Service
metadata:
  name: airdrawer
  namespace: airdrawer
  labels:
    app: airdrawer
spec:
  type: LoadBalancer
  selector:
    app: airdrawer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
